#include <stdio.h>
int main(){
int a, b, temp;
printf("Enter the number to swap: ");
scanf("%d %d"&a,&b);
printf("Number before swaping: "a,b);
temp=a;
a=b;
b=temp;
printf("Number after swaping:a= ,b= "a,b);
return 0;
}
